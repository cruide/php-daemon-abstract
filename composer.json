{
    "name": "cruide/php-daemon-abstract",
    "description": "Abstract PHP daemon base class for Unix/Linux (requires pcntl, posix). Fork, detach, PID management, signal handling, log rotation.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=8.0",
        "ext-pcntl": "*",
        "ext-posix": "*",
        "ext-zlib": "*"
    },
    "require-dev": {
        "phpstan/phpstan": "^1.0"
    },
    "autoload": {
        "psr-4": {
            "Cruide\\PHPDaemon\\": "src/"
        }
    }
}